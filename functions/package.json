{
  "name": "kloof-notifications",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "check": "node --check index.js && node --check lib/booking.js && node --check lib/notifications.js",
    "test": "node --test test/*.test.js",
    "provision:barber": "node scripts/provision_barber.js"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0"
  }
}
